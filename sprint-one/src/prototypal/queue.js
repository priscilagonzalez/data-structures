var Queue = function() {
  // Hey! Rewrite in the new style. Your code will wind up looking very similar,
  // but try not not reference your old code in writing the new style.
  var storage = Object.create(Queue.queueMethods);
  extend(obj, )




};
giraffeMaker.stuff = {}
Class {
  newGiraffe = Object.create(giraffeMaker.stuff)
}

var queueMethods = {};



giraffeMaker.stuff.enqueue = function()
giraffeMaker.stuff.dequeue = function()
enqueue
dequeue
size
